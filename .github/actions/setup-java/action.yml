name: Setup Java composite
description: Sets up a consistent Java version for each workflow
runs:
  using: "composite"
  steps:
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: 17
        distribution: microsoft
