<resources>
    <!-- Applies to the two Activities; Compose content is themed by DemoTheme. -->
    <style name="Theme.RoktDemo" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/rokt_pink</item>
        <item name="colorPrimaryDark">@color/rokt_grey</item>
        <item name="colorAccent">@color/rokt_pink</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
